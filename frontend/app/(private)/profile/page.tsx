import React from "react";
import UserProfile from "@/components/UserProfile/UserProfile";

export const metadata = {
  title: "Profile | StoryMaker",
};

export default function ProfilePage() {
  return <UserProfile />;
}
